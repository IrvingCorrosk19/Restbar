# 12 — PROMPT ENGINE

Templates ES/EN por intent.  
IAiProvider.CompleteAsync(system, user, toolResults) — v1 Deterministic sintetiza markdown.  
Inyección: strip “ignore previous”, deny system role spoof.
