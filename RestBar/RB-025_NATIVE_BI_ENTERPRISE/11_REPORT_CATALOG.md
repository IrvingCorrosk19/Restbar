# 11 — Report Catalog

Driven by `AnalyticsReportCatalog` (~25 report keys with Availability).
HTML shell: `ExecutiveAnalytics/Report` — filters, chart, table, pagination, search, CSV/XLSX/PDF|HTML print.

Not every of the 48 aspirational titles is a separate cshtml; unavailable metrics are explicitly blocked.
