@model IEnumerable<RestBar.Models.Branch>
@using System.Globalization
@{
    ViewData["Title"] = "Sucursales";
}

<!-- Hero Section -->
<section class="hero-section">
    <div class="hero-content">
        <div class="hero-text">
            <h1 class="hero-title">
                <i class="fas fa-store-alt hero-icon"></i>
                Gestión de Sucursales
            </h1>
            <p class="hero-description">
                Administra las sucursales de tu empresa de forma eficiente y organizada
            </p>
        </div>
        <div class="hero-actions">
            @* Botón deshabilitado para administradores - Solo SuperAdmin puede crear sucursales *@
            <button class="btn btn-secondary btn-lg modern-btn" disabled title="Solo SuperAdmin puede crear nuevas sucursales">
                <i class="fas fa-plus me-2"></i>
                Nueva Sucursal
            </button>
        </div>
    </div>
    <div class="hero-decoration">
        <div class="decoration-circle circle-1"></div>
        <div class="decoration-circle circle-2"></div>
        <div class="decoration-circle circle-3"></div>
    </div>
</section>

<!-- Content Section -->
<section class="content-section">
    <div class="container-fluid">
        <!-- Filters Section -->
        <div class="filters-section">
            <div class="row g-3">
                <div class="col-md-4">
                    <div class="search-box">
                        <i class="fas fa-search search-icon"></i>
                        <input type="text" id="searchBranchInput" class="form-control search-input" placeholder="Buscar sucursal...">
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="filter-box">
                        <i class="fas fa-filter filter-icon"></i>
                        <select id="statusFilter" class="form-select filter-select">
                            <option value="">Todos los estados</option>
                            <option value="true">Activa</option>
                            <option value="false">Inactiva</option>
                        </select>
                    </div>
                </div>
                <div class="col-md-4">
                    <div class="d-flex justify-content-end">
                        <button class="btn btn-outline-primary modern-btn" onclick="clearFilters()">
                            <i class="fas fa-eraser me-2"></i>
                            Limpiar Filtros
                        </button>
                    </div>
                </div>
            </div>
        </div>

        <!-- Branches Grid -->
        <div class="content-grid">
            <div class="row g-4" id="branchesGrid">
                @foreach (var branch in Model)
                {
                    <div class="col-xl-3 col-lg-4 col-md-6 col-sm-12">
                        <div class="modern-card branch-card-item">
                            <div class="card-header-modern">
                                <div class="branch-icon">
                                    <i class="fas fa-store-alt"></i>
                                </div>
                                <div class="branch-info">
                                    <h5 class="branch-name">@branch.Name</h5>
                                    <span class="branch-company">@(branch.Company?.Name ?? "Sin compañía")</span>
                                </div>
                                <div class="status-badge">
                                    <span class="badge @(branch.IsActive == true ? "badge-success" : "badge-danger")">
                                        @(branch.IsActive == true ? "Activa" : "Inactiva")
                                    </span>
                                </div>
                            </div>
                            <div class="card-body-modern">
                                <div class="branch-details">
                                    <div class="detail-item">
                                        <i class="fas fa-map-marker-alt detail-icon"></i>
                                        <div class="detail-content">
                                            <span class="detail-label">Dirección</span>
                                            <span class="detail-value">@(!string.IsNullOrWhiteSpace(branch.Address) ? branch.Address : "Sin dirección")</span>
                                        </div>
                                    </div>
                                    <div class="detail-item">
                                        <i class="fas fa-phone detail-icon"></i>
                                        <div class="detail-content">
                                            <span class="detail-label">Teléfono</span>
                                            <span class="detail-value">@(!string.IsNullOrWhiteSpace(branch.Phone) ? branch.Phone : "Sin teléfono")</span>
                                        </div>
                                    </div>
                                    <div class="detail-item">
                                        <i class="fas fa-building detail-icon"></i>
                                        <div class="detail-content">
                                            <span class="detail-label">Compañía</span>
                                            <span class="detail-value">@(branch.Company?.Name ?? "Sin compañía")</span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                            <div class="card-footer-modern">
                                <button class="btn btn-outline-primary btn-sm modern-btn-sm" onclick="editBranch('@branch.Id')" title="Editar">
                                    <i class="fas fa-edit"></i>
                                </button>
                                @* Botón de eliminar deshabilitado para administradores - Solo SuperAdmin puede eliminar sucursales *@
                                <button class="btn btn-outline-secondary btn-sm modern-btn-sm" disabled title="Solo SuperAdmin puede eliminar sucursales">
                                    <i class="fas fa-trash"></i>
                                </button>
                            </div>
                        </div>
                    </div>
                }
            </div>
        </div>

        <!-- Empty State -->
        <div class="empty-state" id="emptyState" style="display: none;">
            <div class="empty-icon">
                <i class="fas fa-store-alt"></i>
            </div>
            <h3 class="empty-title">No se encontraron sucursales</h3>
            <p class="empty-description">Prueba ajustando los filtros de búsqueda o crea una nueva sucursal</p>
            <button class="btn btn-secondary modern-btn" disabled title="Solo SuperAdmin puede crear nuevas sucursales">
                <i class="fas fa-plus me-2"></i>
                Crear Primera Sucursal
            </button>
        </div>
    </div>
</section>

<!-- Modal de Creación -->
<div class="modal fade" id="createBranchModal" tabindex="-1" aria-labelledby="createBranchModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content modern-modal">
            <div class="modal-header modern-modal-header">
                <div class="modal-title-section">
                    <div class="modal-icon">
                        <i class="fas fa-store-alt"></i>
                    </div>
                    <div>
                        <h5 class="modal-title" id="createBranchModalLabel">Nueva Sucursal</h5>
                        <p class="modal-subtitle">Crea una nueva sucursal en el sistema</p>
                    </div>
                </div>
                <button type="button" class="btn-close modern-btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form id="createBranchForm">
                <div class="modal-body modern-modal-body">
                    <div class="row g-3">
                        <div class="col-md-12">
                            <div class="modern-form-group">
                                <label class="form-label">
                                    <i class="fas fa-building me-2"></i>
                                    Compañía
                                </label>
                                <input type="text" class="form-control modern-input" id="currentCompanyDisplay" readonly 
                                       placeholder="Compañía actual" style="background-color: #f8f9fa;">
                                <input type="hidden" name="CompanyId" id="currentCompanyId">
                                <small class="form-text text-muted">
                                    <i class="fas fa-info-circle me-1"></i>
                                    La sucursal se creará automáticamente en tu compañía actual
                                </small>
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="modern-form-group">
                                <label class="form-label">
                                    <i class="fas fa-store-alt me-2"></i>
                                    Nombre de la Sucursal *
                                </label>
                                <input type="text" class="form-control modern-input" name="Name" required 
                                       placeholder="Ingresa el nombre de la sucursal">
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="modern-form-group">
                                <label class="form-label">
                                    <i class="fas fa-map-marker-alt me-2"></i>
                                    Dirección
                                </label>
                                <input type="text" class="form-control modern-input" name="Address" 
                                       placeholder="Ingresa la dirección de la sucursal">
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="modern-form-group">
                                <label class="form-label">
                                    <i class="fas fa-phone me-2"></i>
                                    Teléfono
                                </label>
                                <input type="text" class="form-control modern-input" name="Phone" 
                                       placeholder="Ingresa el número de teléfono">
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="modern-form-group">
                                <div class="form-check modern-check">
                                    <input type="checkbox" class="form-check-input" name="IsActive" checked>
                                    <label class="form-check-label">
                                        <i class="fas fa-check-circle me-2"></i>
                                        Sucursal Activa
                                    </label>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer modern-modal-footer">
                    <button type="button" class="btn btn-outline-secondary modern-btn" data-bs-dismiss="modal">
                        <i class="fas fa-times me-2"></i>
                        Cancelar
                    </button>
                    <button type="button" class="btn btn-primary modern-btn" onclick="createBranch()">
                        <i class="fas fa-save me-2"></i>
                        Crear Sucursal
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

<!-- Modal de Edición -->
<div class="modal fade" id="editBranchModal" tabindex="-1" aria-labelledby="editBranchModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content modern-modal">
            <div class="modal-header modern-modal-header">
                <div class="modal-title-section">
                    <div class="modal-icon">
                        <i class="fas fa-edit"></i>
                    </div>
                    <div>
                        <h5 class="modal-title" id="editBranchModalLabel">Editar Sucursal</h5>
                        <p class="modal-subtitle">Modifica la información de la sucursal</p>
                    </div>
                </div>
                <button type="button" class="btn-close modern-btn-close" data-bs-dismiss="modal"></button>
            </div>
            <form id="editBranchForm">
                <div class="modal-body modern-modal-body">
                    <input type="hidden" name="Id">
                    <div class="row g-3">
                                                 <div class="col-md-12">
                             <div class="modern-form-group">
                                 <label class="form-label">
                                     <i class="fas fa-building me-2"></i>
                                     Compañía
                                 </label>
                                 <input type="text" class="form-control modern-input" id="editCompanyDisplay" readonly 
                                        placeholder="Compañía actual" style="background-color: #f8f9fa;">
                                 <input type="hidden" name="CompanyId" id="editCompanyId">
                                 <small class="form-text text-muted">
                                     <i class="fas fa-info-circle me-1"></i>
                                     La compañía se mantiene automáticamente de tu usuario actual
                                 </small>
                             </div>
                         </div>
                        <div class="col-md-12">
                            <div class="modern-form-group">
                                <label class="form-label">
                                    <i class="fas fa-store-alt me-2"></i>
                                    Nombre de la Sucursal *
                                </label>
                                <input type="text" class="form-control modern-input" name="Name" required 
                                       placeholder="Ingresa el nombre de la sucursal">
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="modern-form-group">
                                <label class="form-label">
                                    <i class="fas fa-map-marker-alt me-2"></i>
                                    Dirección
                                </label>
                                <input type="text" class="form-control modern-input" name="Address" 
                                       placeholder="Ingresa la dirección de la sucursal">
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="modern-form-group">
                                <label class="form-label">
                                    <i class="fas fa-phone me-2"></i>
                                    Teléfono
                                </label>
                                <input type="text" class="form-control modern-input" name="Phone" 
                                       placeholder="Ingresa el número de teléfono">
                            </div>
                        </div>
                        <div class="col-md-12">
                            <div class="modern-form-group">
                                <div class="form-check modern-check">
                                    <input type="checkbox" class="form-check-input" name="IsActive">
                                    <label class="form-check-label">
                                        <i class="fas fa-check-circle me-2"></i>
                                        Sucursal Activa
                                    </label>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
                <div class="modal-footer modern-modal-footer">
                    <button type="button" class="btn btn-outline-secondary modern-btn" data-bs-dismiss="modal">
                        <i class="fas fa-times me-2"></i>
                        Cancelar
                    </button>
                    <button type="button" class="btn btn-primary modern-btn" onclick="updateBranch()">
                        <i class="fas fa-save me-2"></i>
                        Guardar Cambios
                    </button>
                </div>
            </form>
        </div>
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        // Función para limpiar filtros
        function clearFilters() {
            $("#searchBranchInput").val("");
            $("#statusFilter").val("");
            filterBranches();
        }

        // Función para actualizar estado vacío
        function updateEmptyState() {
            const visibleCards = $("#branchesGrid .col:visible").length;
            if (visibleCards === 0) {
                $("#emptyState").show();
            } else {
                $("#emptyState").hide();
            }
        }

        // Función para filtrar sucursales
        function filterBranches() {
            const searchTerm = $('#searchBranchInput').val().toLowerCase();
            const status = $('#statusFilter').val();
            
            $('#branchesGrid .col').each(function () {
                const card = $(this);
                const name = card.find('.branch-name').text().toLowerCase();
                const isActive = card.find('.badge-success').length > 0;
                
                const matchesSearch = name.includes(searchTerm);
                const matchesStatus = status === '' || (status === 'true' && isActive) || (status === 'false' && !isActive);
                
                card.toggle(matchesSearch && matchesStatus);
            });
            
            updateEmptyState();
        }

                 // Función para cargar la compañía actual del usuario (para creación)
         function loadCurrentUserCompany() {
            
            
            $.get('/User/GetCurrentUser', function (res) {
                if (res.success && res.data) {
                    
                    
                    if (res.data.branch && res.data.branch.company) {
                        $('#currentCompanyDisplay').val(res.data.branch.company.name);
                        $('#currentCompanyId').val(res.data.branch.company.id);
                        
                        
                        
                        // Habilitar botón de crear
                        $('#createBranchModal').find('button.btn-primary').prop('disabled', false);
                    } else {
                        
                        $('#currentCompanyDisplay').val('Error: Usuario sin compañía asignada');
                        $('#currentCompanyId').val('');
                    }
                } else {
                    
                    $('#currentCompanyDisplay').val('Error al cargar compañía');
                    $('#currentCompanyId').val('');
                }
            }).fail(function(error) {
                
                $('#currentCompanyDisplay').val('Error de conexión');
                $('#currentCompanyId').val('');
            });
                 }

         // Función para cargar la compañía actual del usuario (para edición)
         function loadCurrentUserCompanyForEdit() {
             
             
             $.get('/User/GetCurrentUser', function (res) {
                 if (res.success && res.data) {
                     
                     
                     if (res.data.branch && res.data.branch.company) {
                         $('#editCompanyDisplay').val(res.data.branch.company.name);
                         $('#editCompanyId').val(res.data.branch.company.id);
                         
                         
                     } else {
                         
                         $('#editCompanyDisplay').val('Error: Usuario sin compañía asignada');
                         $('#editCompanyId').val('');
                     }
                 } else {
                     
                     $('#editCompanyDisplay').val('Error al cargar compañía');
                     $('#editCompanyId').val('');
                 }
             }).fail(function(error) {
                 
                 $('#editCompanyDisplay').val('Error de conexión');
                 $('#editCompanyId').val('');
             });
         }

                  // Función para cargar compañías en selects (para edición) - YA NO SE USA
         /*
         function loadCompaniesSelect(selectId, selectedId) {
             
             
             var select = $(selectId);
             select.empty().append('<option value="">Cargando compañías...</option>').prop('disabled', true);
             
             $.get('/User/GetCompanies', function (res) {
                 
                 
                 if (res.success) {
                     select.empty();
                     select.append('<option value="">Seleccione una compañía</option>');
                     if (res.data && Array.isArray(res.data)) {
                         
                         
                         res.data.forEach(function (c) {
                             var option = $('<option value="' + c.id + '">' + c.name + '</option>');
                             if (selectedId && c.id === selectedId) {
                                 option.prop('selected', true);
                                 
                             }
                             select.append(option);
                         });
                     }
                     select.prop('disabled', false);
                     
                     // Forzar actualización del valor seleccionado
                     if (selectedId) {
                         select.val(selectedId);
                         select.trigger('change');
                     }
                     
                     if (selectId === '#createCompanyId') {
                         $('#createBranchModal').find('button.btn-primary').prop('disabled', false);
                     }
                     
                     
                 } else {
                     
                     select.empty().append('<option value="">No hay compañías disponibles</option>').prop('disabled', true);
                 }
             }).fail(function(error) {
                 
                 select.empty().append('<option value="">Error al cargar compañías</option>').prop('disabled', true);
             });
         }
         */

        // Eventos
        $(document).ready(function () {
            $('#searchBranchInput').on('keyup', filterBranches);
            $('#statusFilter').on('change', filterBranches);
            updateEmptyState();
        });

        // Eventos de modales
        $('#createBranchModal').on('show.bs.modal', function () {
            var btn = $(this).find('button.btn-primary');
            btn.prop('disabled', true);
            
            // Cargar información de la compañía actual del usuario
            loadCurrentUserCompany();
        });

        // Evento eliminado - la carga de compañías se maneja en editBranch()

        // Función para crear sucursal
        window.createBranch = function () {
            const form = $('#createBranchForm');
            const formData = {};
            let errors = [];
            
            form.serializeArray().forEach(item => {
                formData[item.name] = item.value;
            });
            formData.IsActive = form.find('[name="IsActive"]').is(':checked');
            
            // Validaciones
            if (!formData.Name || formData.Name.trim() === "") {
                errors.push('El nombre es requerido');
            }
            // CompanyId se asigna automáticamente del usuario actual
            
            if (errors.length > 0) {
                Swal.fire({
                    icon: 'error',
                    title: 'Error de validación',
                    html: errors.join('<br>')
                });
                return;
            }
            
            Swal.fire({
                title: 'Creando sucursal...',
                text: 'Por favor espera un momento',
                icon: 'info',
                allowOutsideClick: false,
                showConfirmButton: false,
                didOpen: () => {
                    Swal.showLoading();
                }
            });
            
            $.ajax({
                url: '/Branch/Create',
                method: 'POST',
                contentType: 'application/json',
                data: JSON.stringify(formData),
                success: function (response) {
                    if (response.success) {
                        $('#createBranchModal').modal('hide');
                        Swal.fire({
                            icon: 'success',
                            title: '¡Éxito!',
                            text: '¡Sucursal creada correctamente!',
                            showConfirmButton: false,
                            timer: 1500
                        }).then(() => {
                            location.reload();
                        });
                    } else {
                        Swal.fire({
                            icon: 'error',
                            title: 'Error',
                            text: response.message || 'No se pudo crear la sucursal'
                        });
                    }
                },
                error: function (xhr, status, error) {
                    Swal.fire({
                        icon: 'error',
                        title: 'Error',
                        text: 'No se pudo crear la sucursal. Por favor intenta nuevamente.'
                    });
                }
            });
        }

        // Función para editar sucursal
        window.editBranch = function (id) {
            Swal.fire({
                title: 'Cargando información...',
                text: 'Por favor espera un momento',
                icon: 'info',
                allowOutsideClick: false,
                showConfirmButton: false,
                didOpen: () => {
                    Swal.showLoading();
                }
            });
            
            $.ajax({
                url: `/Branch/Get/${id}`,
                method: 'GET',
                success: function (response) {
                    if (response.success) {
                        const branch = response.data;
                        const form = $('#editBranchForm');
                        
                        // Limpiar y llenar el formulario
                        form[0].reset();
                        form.find('[name="Id"]').val(branch.id);
                        form.find('[name="Name"]').val(branch.name);
                        form.find('[name="Address"]').val(branch.address || '');
                        form.find('[name="Phone"]').val(branch.phone || '');
                        form.find('[name="IsActive"]').prop('checked', branch.isActive);
                        
                                                 // Cargar la compañía actual del usuario
                         loadCurrentUserCompanyForEdit();
                        
                        // Cerrar el loading y mostrar el modal
                        Swal.close();
                        const editModal = new bootstrap.Modal(document.getElementById('editBranchModal'));
                        editModal.show();
                    } else {
                        Swal.close();
                        Swal.fire({
                            icon: 'error',
                            title: 'Error',
                            text: response.message || 'Error al cargar la sucursal'
                        });
                    }
                },
                error: function (xhr, status, error) {
                    Swal.close();
                    Swal.fire({
                        icon: 'error',
                        title: 'Error',
                        text: 'Error al cargar la sucursal. Por favor intenta nuevamente.'
                    });
                }
            });
        }

        // Función para actualizar sucursal
        window.updateBranch = function () {
            const form = $('#editBranchForm');
            const formData = {};
            let errors = [];
            
            form.serializeArray().forEach(item => {
                formData[item.name] = item.value;
            });
            formData.IsActive = form.find('[name="IsActive"]').is(':checked');
            
                         // Validaciones
             if (!formData.Name || formData.Name.trim() === "") {
                 errors.push('El nombre es requerido');
             }
             // CompanyId se asigna automáticamente del usuario actual
            
            if (errors.length > 0) {
                Swal.fire({
                    icon: 'error',
                    title: 'Error de validación',
                    html: errors.join('<br>')
                });
                return;
            }
            
            Swal.fire({
                title: 'Actualizando sucursal...',
                text: 'Por favor espera un momento',
                icon: 'info',
                allowOutsideClick: false,
                showConfirmButton: false,
                didOpen: () => {
                    Swal.showLoading();
                }
            });
            
            $.ajax({
                url: `/Branch/Edit/${formData.Id}`,
                method: 'PUT',
                contentType: 'application/json',
                data: JSON.stringify(formData),
                success: function (response) {
                    if (response.success) {
                        $('#editBranchModal').modal('hide');
                        Swal.fire({
                            icon: 'success',
                            title: '¡Éxito!',
                            text: '¡Sucursal editada correctamente!',
                            showConfirmButton: false,
                            timer: 1500
                        }).then(() => {
                            location.reload();
                        });
                    } else {
                        Swal.fire({
                            icon: 'error',
                            title: 'Error',
                            text: response.message || 'Error al actualizar la sucursal'
                        });
                    }
                },
                error: function (xhr, status, error) {
                    Swal.fire({
                        icon: 'error',
                        title: 'Error',
                        text: 'Error al actualizar la sucursal. Por favor intenta nuevamente.'
                    });
                }
            });
        }

        // Función para eliminar sucursal
        window.deleteBranch = function (id) {
            Swal.fire({
                title: '¿Estás seguro?',
                text: 'Esta acción eliminará la sucursal permanentemente',
                icon: 'warning',
                showCancelButton: true,
                confirmButtonColor: 'var(--danger-color)',
                cancelButtonColor: 'var(--secondary-color)',
                confirmButtonText: '<i class="fas fa-trash me-2"></i>Sí, eliminar',
                cancelButtonText: '<i class="fas fa-times me-2"></i>Cancelar',
                reverseButtons: true
            }).then((result) => {
                if (result.isConfirmed) {
                    Swal.fire({
                        title: 'Eliminando sucursal...',
                        text: 'Por favor espera un momento',
                        icon: 'info',
                        allowOutsideClick: false,
                        showConfirmButton: false,
                        didOpen: () => {
                            Swal.showLoading();
                        }
                    });
                    
                    $.ajax({
                        url: `/Branch/Delete/${id}`,
                        method: 'DELETE',
                        success: function (response) {
                            if (response.success) {
                                Swal.fire({
                                    icon: 'success',
                                    title: '¡Eliminada!',
                                    text: '¡Sucursal eliminada correctamente!',
                                    showConfirmButton: false,
                                    timer: 1500
                                }).then(() => {
                                    location.reload();
                                });
                            } else {
                                Swal.fire({
                                    icon: 'error',
                                    title: 'Error',
                                    text: response.message || 'No se pudo eliminar la sucursal'
                                });
                            }
                        },
                        error: function (xhr, status, error) {
                            Swal.fire({
                                icon: 'error',
                                title: 'Error',
                                text: 'Error al eliminar la sucursal. Por favor intenta nuevamente.'
                            });
                        }
                    });
                }
            });
        }
    </script>
}

@section Styles {
    <style>
        :root {
            --primary-color: #2c3e50;
            --secondary-color: #34495e;
            --accent-color: #e67e22;
            --success-color: #27ae60;
            --danger-color: #e74c3c;
            --warning-color: #f39c12;
            --info-color: #3498db;
            --light-color: #ecf0f1;
            --dark-color: #2c3e50;
            --border-radius: 20px;
            --transition: all 0.3s ease;
            --box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
            --hover-shadow: 0 20px 60px rgba(0, 0, 0, 0.15);
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
            min-height: 100vh;
            line-height: 1.6;
        }

        /* Hero Section */
        .hero-section {
            background: linear-gradient(135deg, var(--primary-color) 0%, var(--secondary-color) 100%);
            color: white;
            padding: 80px 0;
            position: relative;
            overflow: hidden;
        }

        .hero-content {
            container: 1200px;
            max-width: 1200px;
            margin: 0 auto;
            padding: 0 20px;
            display: flex;
            justify-content: space-between;
            align-items: center;
            position: relative;
            z-index: 2;
        }

        .hero-text h1 {
            font-size: 3rem;
            font-weight: 700;
            margin-bottom: 20px;
            display: flex;
            align-items: center;
        }

        .hero-icon {
            background: linear-gradient(45deg, var(--accent-color), var(--warning-color));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            background-clip: text;
            margin-right: 20px;
        }

        .hero-description {
            font-size: 1.2rem;
            opacity: 0.9;
            max-width: 600px;
        }

        .hero-actions {
            display: flex;
            gap: 15px;
        }

        .hero-decoration {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
            z-index: 1;
        }

        .decoration-circle {
            position: absolute;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.1);
            animation: float 6s ease-in-out infinite;
        }

        .circle-1 {
            width: 100px;
            height: 100px;
            top: 10%;
            right: 10%;
            animation-delay: 0s;
        }

        .circle-2 {
            width: 150px;
            height: 150px;
            top: 60%;
            right: 5%;
            animation-delay: 2s;
        }

        .circle-3 {
            width: 80px;
            height: 80px;
            top: 30%;
            right: 30%;
            animation-delay: 4s;
        }

        @@keyframes float {
            0%, 100% { transform: translateY(0px); }
            50% { transform: translateY(-20px); }
        }

        /* Content Section */
        .content-section {
            padding: 40px 0;
            background: var(--light-color);
            min-height: calc(100vh - 200px);
        }

        /* Filters Section */
        .filters-section {
            background: white;
            padding: 30px;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            margin-bottom: 30px;
        }

        .search-box, .filter-box {
            position: relative;
        }

        .search-icon, .filter-icon {
            position: absolute;
            left: 15px;
            top: 50%;
            transform: translateY(-50%);
            color: var(--secondary-color);
            z-index: 2;
        }

        .search-input, .filter-select {
            padding-left: 45px;
            border: 2px solid #e0e0e0;
            border-radius: 15px;
            font-size: 16px;
            transition: var(--transition);
        }

        .search-input:focus, .filter-select:focus {
            border-color: var(--accent-color);
            box-shadow: 0 0 0 3px rgba(230, 126, 34, 0.1);
        }

        /* Content Grid */
        .content-grid {
            margin-top: 30px;
        }

        /* Modern Cards */
        .modern-card {
            background: white;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
            transition: var(--transition);
            overflow: hidden;
            height: 100%;
            display: flex;
            flex-direction: column;
        }

        .modern-card:hover {
            transform: translateY(-10px);
            box-shadow: var(--hover-shadow);
        }

        .card-header-modern {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 25px;
            position: relative;
        }

        .branch-icon {
            width: 60px;
            height: 60px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
            margin-bottom: 15px;
        }

        .branch-info h5 {
            margin: 0;
            font-size: 1.3rem;
            font-weight: 600;
        }

        .branch-company {
            opacity: 0.8;
            font-size: 0.9rem;
        }

        .status-badge {
            position: absolute;
            top: 15px;
            right: 15px;
        }

        .badge-success {
            background: var(--success-color);
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
        }

        .badge-danger {
            background: var(--danger-color);
            color: white;
            padding: 8px 16px;
            border-radius: 20px;
            font-size: 0.8rem;
            font-weight: 600;
        }

        .card-body-modern {
            padding: 25px;
            flex: 1;
        }

        .branch-details {
            display: flex;
            flex-direction: column;
            gap: 15px;
        }

        .detail-item {
            display: flex;
            align-items: center;
            gap: 12px;
            padding: 10px 0;
            border-bottom: 1px solid #f0f0f0;
        }

        .detail-item:last-child {
            border-bottom: none;
        }

        .detail-icon {
            width: 20px;
            color: var(--accent-color);
        }

        .detail-content {
            flex: 1;
        }

        .detail-label {
            display: block;
            font-size: 0.85rem;
            color: #666;
            margin-bottom: 2px;
        }

        .detail-value {
            font-weight: 600;
            color: var(--dark-color);
        }

        .card-footer-modern {
            padding: 20px 25px;
            background: #f8f9fa;
            border-top: 1px solid #e0e0e0;
            display: flex;
            justify-content: center;
            gap: 10px;
        }

        /* Modern Buttons */
        .modern-btn {
            border: none;
            border-radius: 15px;
            padding: 12px 30px;
            font-weight: 600;
            text-transform: uppercase;
            letter-spacing: 0.5px;
            transition: var(--transition);
            position: relative;
            overflow: hidden;
        }

        .modern-btn::before {
            content: '';
            position: absolute;
            top: 50%;
            left: 50%;
            width: 0;
            height: 0;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            transform: translate(-50%, -50%);
            transition: var(--transition);
        }

        .modern-btn:hover::before {
            width: 300px;
            height: 300px;
        }

        .modern-btn-sm {
            padding: 8px 15px;
            font-size: 0.9rem;
            border-radius: 10px;
        }

        .btn-primary {
            background: linear-gradient(135deg, var(--accent-color), var(--warning-color));
            color: white;
        }

        .btn-primary:hover {
            background: linear-gradient(135deg, var(--warning-color), var(--accent-color));
            transform: translateY(-2px);
        }

        .btn-outline-primary {
            border: 2px solid var(--accent-color);
            color: var(--accent-color);
        }

        .btn-outline-primary:hover {
            background: var(--accent-color);
            color: white;
        }

        .btn-outline-danger {
            border: 2px solid var(--danger-color);
            color: var(--danger-color);
        }

        .btn-outline-danger:hover {
            background: var(--danger-color);
            color: white;
        }

        /* Empty State */
        .empty-state {
            text-align: center;
            padding: 80px 20px;
            background: white;
            border-radius: var(--border-radius);
            box-shadow: var(--box-shadow);
        }

        .empty-icon {
            width: 120px;
            height: 120px;
            margin: 0 auto 30px;
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 48px;
            color: white;
        }

        .empty-title {
            font-size: 2rem;
            color: var(--primary-color);
            margin-bottom: 15px;
        }

        .empty-description {
            color: #666;
            font-size: 1.1rem;
            margin-bottom: 30px;
        }

        /* Modern Modals */
        .modern-modal .modal-content {
            border: none;
            border-radius: var(--border-radius);
            box-shadow: var(--hover-shadow);
        }

        .modern-modal-header {
            background: linear-gradient(135deg, var(--primary-color), var(--secondary-color));
            color: white;
            padding: 30px;
            border-radius: var(--border-radius) var(--border-radius) 0 0;
        }

        .modal-title-section {
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .modal-icon {
            width: 60px;
            height: 60px;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 24px;
        }

        .modal-title {
            margin: 0;
            font-size: 1.5rem;
            font-weight: 600;
        }

        .modal-subtitle {
            margin: 5px 0 0 0;
            opacity: 0.8;
            font-size: 0.95rem;
        }

        .modern-btn-close {
            background: rgba(255, 255, 255, 0.2);
            border: none;
            border-radius: 50%;
            width: 40px;
            height: 40px;
            display: flex;
            align-items: center;
            justify-content: center;
            transition: var(--transition);
        }

        .modern-btn-close:hover {
            background: rgba(255, 255, 255, 0.3);
        }

        .modern-modal-body {
            padding: 40px;
        }

        .modern-form-group {
            margin-bottom: 25px;
        }

        .modern-form-group label {
            font-weight: 600;
            color: var(--primary-color);
            margin-bottom: 10px;
            display: flex;
            align-items: center;
        }

        .modern-input, .modern-select {
            border: 2px solid #e0e0e0;
            border-radius: 15px;
            padding: 15px 20px;
            font-size: 16px;
            transition: var(--transition);
            width: 100%;
        }

        .modern-input:focus, .modern-select:focus {
            border-color: var(--accent-color);
            box-shadow: 0 0 0 3px rgba(230, 126, 34, 0.1);
        }

        .modern-check {
            display: flex;
            align-items: center;
            gap: 10px;
            padding: 15px 20px;
            background: #f8f9fa;
            border-radius: 15px;
            border: 2px solid #e0e0e0;
            transition: var(--transition);
        }

        .modern-check:hover {
            border-color: var(--accent-color);
        }

        .modern-check .form-check-input {
            width: 20px;
            height: 20px;
            margin: 0;
        }

        .modern-check .form-check-label {
            font-weight: 500;
            color: var(--dark-color);
            margin: 0;
        }

        .modern-modal-footer {
            padding: 30px 40px;
            background: #f8f9fa;
            border-top: 1px solid #e0e0e0;
            display: flex;
            justify-content: end;
            gap: 15px;
        }

        /* Responsive Design */
        @@media (max-width: 768px) {
            .hero-content {
                flex-direction: column;
                text-align: center;
                gap: 30px;
            }

            .hero-text h1 {
                font-size: 2.5rem;
            }

            .hero-actions {
                justify-content: center;
            }

            .filters-section {
                padding: 20px;
            }

            .modern-modal-body {
                padding: 30px 20px;
            }

            .modern-modal-footer {
                padding: 20px;
            }
        }

        @@media (max-width: 480px) {
            .hero-text h1 {
                font-size: 2rem;
            }

            .hero-description {
                font-size: 1rem;
            }

            .branch-details {
                gap: 10px;
            }

            .detail-item {
                padding: 8px 0;
            }
        }
    </style>
} 